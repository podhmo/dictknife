paths:
  /users3:
    post:
      summary: Craetes a user.
      parameters:
        - $ref: "components/parameters.yaml#/parameters/postUserBody"
      responses:
        "201":
          description: Created
