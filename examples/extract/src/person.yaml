definitions:
  people:
    type: array
    items:
      $ref: "#/definitions/person"
  person:
    type: object
    properties:
      name:
        $ref: "./primitive.yaml#/definitions/name"
      age:
        $ref: "./primitive.yaml#/definitions/age"
      group:
        $ref: "./group.yaml"
