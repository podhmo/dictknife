definitions:
  config:
    properties:
      db:
        $ref: '#/definitions/db'
    type: object
  db:
    type: object
    properties:
      host:
        type: string
        example: localhost
      name:
        type: string
        example: xxx
      port:
        type: integer
        example: 444
    required:
    - host
    - name
    - port
