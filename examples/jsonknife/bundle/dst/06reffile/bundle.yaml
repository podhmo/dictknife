components:
  schemas:
    user:
      $ref: '#/components/schema/user'
  schema:
    user:
      type: object
      propreties:
        name:
          type: string
        age:
          type: integer
